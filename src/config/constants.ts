export const TOKEN = 'SQUAD_BLOG_TOKEN'

export enum ThemeMode {
  light = 'light',
  dark = 'dark'
}

export enum Size {
  default = 'default',
  middle = 'middle',
  small = 'small'
};

export enum MenuType {
  '菜单' = 1,
  '权限' = 2
}

export enum BatchSelector {
  all,
  selected
}

export enum WebsiteStatus {
  '正常状态',
  '升级维护',
  '网站已关闭',
  '置灰状态'
}

export const ALL_PICTURES_CATEGORY_NAME = '全部图片'
